---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: snort-debian-stable
    image: debian:stable
provisioner:
  name: ansible
  inventory:
    group_vars:
      all:
        snort_ignore_docker: no
  lint:
    name: ansible-lint
scenario:
  name: debian-stable
